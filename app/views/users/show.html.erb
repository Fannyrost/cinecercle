<div class="personnalized-container text-center my-5">
  <div>
  <h1 class="p-2 my-5">Hello <%= @user.pseudo.capitalize %> ! </h1>

  <div class="d-flex justify-content-between align-items-center my-2" style="width: 400px; margin: auto;">
    <div><p><i class="fas fa-users pink-font"></i> <%= @user.memberships.count %> cercles de partage </p></div>
    <div><p><i class="fas fa-film pink-font"></i> <%= @user.reco_count %> films recommandés</p></div>

  </div>
  <%= render 'shared/moviebrary', user: @user %>
</div>
