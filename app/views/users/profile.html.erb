<div class="personnalized-container  my-5">
  <div>
    <div class="d-flex justify-content-end my-5">
      <div>
        <%= render 'shared/btn_underline', fontawesome: "fas fa-user-edit", title: "Editer mon profil", path: edit_user_registration_path(@user) %>
      </div>
    </div>

    <div class="d-flex justify-content-center align-items-center">
      <div>
        <%= cl_image_tag @user.photo.key, class: "avatar-large" %>
      </div>
      <div class="ml-5">
        <h1 class="p-2 mt-5">Hello <%= @user.pseudo.capitalize%> ! </h1>
          <div><p><i class="fas fa-users pink-font"></i> <%= @user.memberships.count %> cercles de partage </p></div>
          <div><p><i class="fas fa-film pink-font"></i> <%= @user.reco_count %> films recommandés</p></div>
      </div>
    </div>






  <%= render 'shared/moviebrary', user: @user  %>
  </div>
</div>
